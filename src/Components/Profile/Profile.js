import styles from "./Profile.module.css";

const Profile = ({}) => {
  return <div className={styles["profile-layout"]}>
    <h1>This is the profile page</h1>
  </div>
};

export default Profile;
